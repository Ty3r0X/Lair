# Zola configuration file

base_url = "/"
title = "Ty3r0X's Lair"
description = "Follow The Eye! - Personal website of Ty3r0X"

# The site author info: used in feeds by default.
author = "Ty3r0X"

# The default language; used in feeds.
default_language = "en"

# For overriding the default output directory `public`, set it to another value (e.g.: "docs")
output_dir = "public"

# When set to "true", the Sass files in the `sass` directory in the site root are compiled.
compile_sass = true

# XHTML hates minified HTML
minify_html = false

# A list of glob patterns specifying asset files to ignore when the content
# directory is processed. Defaults to none, which means that all asset files are
# copied over to the `public` directory.
ignored_content = []

# When set to "true", feeds are automatically generated.
generate_feeds = true

# The filenames to use for the feeds. Used as the template filenames, too.
feed_filenames = ["atom.xml"]

# The number of articles to include in the feed. All items are included if
# this limit is not set (the default).
# feed_limit = 20

# When set to "true", files in the `static` directory are hard-linked. Useful for large
# static files. Note that for this to work, both `static` and the
# output directory need to be on the same filesystem. Note that the theme's `static`
# files are always copied.
hard_link_static = true

# The taxonomies to be rendered for the site and their configuration of the default languages
taxonomies = [{ name = "tags", feed = true }]

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Custom variables can be defined here for use in templates
nav_links = [
  { name = "Chaox", url = "https://chaox.ro/" },
  { name = "My Blog", url = "/blog/" },
  { name = "Webrings", url = "/webrings/" },
]

# Footer links
footer_links = [
  { name = "Neocities Mirror", url = "https://ty3r0x.neocities.org/" },
]
